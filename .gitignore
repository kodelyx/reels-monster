# ══════════════════════════════════════════════════════════════════════════════
# SECRETS — never commit (live account session cookies)
# ══════════════════════════════════════════════════════════════════════════════
vendor/*/cookies.json      # live ChatGPT / Google account login — never commit
*.pem
*.key
# NOTE: root config.env IS committed — AI_API_KEY is a free-model key (cc.freemodel.dev),
# no billing. vendor/flow/config.env too (no secrets, just project ID + ports).

# ─── runtime workspace (regenerated per video, never commit) ──────────────────
project/
output/
vendor/*/output/

# ─── bundled Flow server runtime (Python/uv) — regenerated, don't commit ──────
vendor/flow/.venv/
vendor/flow/uv.lock

# ─── personal assets (privacy — don't publish) ────────────────────────────────
profile/avatar.jpg
profile/profile.json
profile/topic_history.json

# ─── remotion build artifacts ─────────────────────────────────────────────────
remotion/node_modules/
remotion/out/
remotion/public/avatar
remotion/public/broll
remotion/public/sfx
remotion/public/bg_music.mp3
remotion/props/caption.json

# ─── logs / runtime cruft ─────────────────────────────────────────────────────
vendor/*/server.log
*.log

# ─── python / editor / os cruft ───────────────────────────────────────────────
__pycache__/
*.pyc
.DS_Store
.claude/
.cc-writes/
